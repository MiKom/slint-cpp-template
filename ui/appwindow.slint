import { Button, VerticalBox } from "std-widgets.slint";
import { Theme, SubTheme } from "./theme.slint";

// Interestingly, if I move the globals Theme and SubTheme here, it won't crash

export component AppWindow inherits Window {
    init => {
        Theme.font-family = SubTheme.font-family;
    }
}
